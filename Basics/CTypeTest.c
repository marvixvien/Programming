#include<stdio.h>
#include<ctype.h>
main()
/*
{
	char character;
	printf("Press any key: \n");
	character = getchar();
	if(isalnum(character) > 0)
	printf("The character is a alphanumeric.");
	else
	printf("the character is not ...");
} 
*/

/*
{
	char character;
	printf("Press any key: \n");
	character = getchar();
	if(isalpha(character) > 0)
	printf("The character is a alphabet.");
	else
	printf("the character is not ...");
} 
*/

/*
{
	char character;
	printf("Press any key: \n");
	character = getchar();
	if(isdigit(character) > 0)
	printf("The character is a digit.");
	else
	printf("the character is not ...");
} 
*/

/*
{
	char character;
	printf("Press any key: \n");
	character = getchar();
	if(isprint(character) > 0)
	printf("The character is a printable char.");
	else
	printf("the character is not ...");
} 
*/

/*
{
	char character;
	printf("Press any key: \n");
	character = getchar();
	if(ispunct(character) > 0)
	printf("The character is a punctuation.");
	else
	printf("the character is not ...");
} 
*/

/*
{
	char character;
	printf("Press any key: \n");
	character = getchar();
	if(isspace(character) > 0)
	printf("The character is a spacebar.");
	else
	printf("the character is not ...");
} 
*/

/*
{
	char character;
	printf("Press any key: \n");
	character = getchar();
	if(islower(character) > 0)
	printf("The character is a lower case letter.");
	else
	printf("the character is not ...");
} 
*/

/*
{
	char character;
	printf("Press any key: \n");
	character = getchar();
	if(isupper(character) > 0)
	printf("The character is a uppercase letter.");
	else
	printf("the character is not ...");
} 
*/
